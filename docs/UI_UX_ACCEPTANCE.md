# Remaining UI/UX repair

Baseline: `55961a0d09357a04be104141683cff39039d857d` (current main at inspection).

AGENTS.md read. Existing local source validation: 55 files. Existing Node tests: 92 passed, 0 failed, 0 skipped. A documentation-only PR revision runs the unchanged Windows renderer and portable workflow before application edits. Previous Windows build 174 and its source/evidence artifacts were also inspected; these are baseline evidence, not evidence for the new repair.

Scope: intentional keyboard shortcut capture and transactional registration with native recovery; request-scoped error recovery; usable small interview workspace; start-first Home; truthful titled History; keyboard/modal/label semantics; consistent save failure handling; theme/reduced-motion polish.

Ponytail skill resources were not exposed by the available skill catalog or plugin search. Apply the requested fallback principles without claiming Ponytail execution: trace callers, repair owners, reuse native controls, remove migrated code, preserve validation/cancellation/security/data, and measure runtime/test/dependency changes separately.

No merge, release or account changes are authorized. Physical Windows 10/11 DPI, compositor transparency, assistive technologies, real audio devices and authenticated provider quotas require separate device/account acceptance. Windows Server CI and forced Chromium scaling are explicitly different.

Implementation and exact final evidence will be recorded here before delivery.
