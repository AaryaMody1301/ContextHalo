[{"path":"src/utils/hubMetadata.js","before":null,"content":"// Hugging Face tree listings are paginated; never assume the first page contains\n// either the requested quantization or its paired multimodal projector.\nasync function listModelFiles(repository, signal) {\n    const encoded = repository.split('/').map(encodeURIComponent).join('/');\n    const prefix = `https://huggingface.co/api/models/${encoded}/tree/`;\n    let url = `${prefix}main?recursive=true&expand=true&limit=100`;\n    const seen = new Set();\n    const files = [];\n    for (let page = 0; url && page < 50; page++) {\n        if (!url.startsWith(prefix) || seen.has(url)) throw new Error('Invalid model pagination response');\n        seen.add(url);\n        const timeout = AbortSignal.timeout(15000);\n        const combined = signal ? AbortSignal.any([signal, timeout]) : timeout;\n        const response = await fetch(url, { signal: combined });\n        if (!response.ok) throw new Error(`Could not inspect Hugging Face model: HTTP ${response.status}`);\n        const body = await response.json();\n        if (!Array.isArray(body)) throw new Error('Invalid Hugging Face model metadata');\n        files.push(...body);\n        const link = response.headers?.get?.('link') || '';\n        const next = link.split(',').find(part => /rel=[\"']?next[\"']?/.test(part));\n        url = next?.match(/<([^>]+)>/)?.[1] || '';\n    }\n    if (url) throw new Error('Model repository is too large to inspect safely');\n    return files;\n}\nfunction selectProjector(files) {\n    for (const name of ['mmproj-BF16.gguf', 'mmproj-F16.gguf', 'mmproj-F32.gguf']) {\n        const file = files.find(item => item.type === 'file' && item.path === name);\n        if (file) return file;\n    }\n    throw new Error('The selected vision model does not provide a supported mmproj projector');\n}\nmodule.exports = { listModelFiles, selectProjector };\n","after":"87fa68c579c98b75e4d747983e50ba77723d776df2283040e02d7d2d575d906a"},{"path":"src/utils/localai.js","before":"38d10c773c252b68af3048589ebf9518e47b6ff6aa0d9bd9ffcf4e1670469730","edits":[[27,28,""],[299,331,""],[379,380,"async function startWhisperServer(executablePath, modelPath, signal) {\n"],[388,389,"    await waitForServer(`${whisperBaseUrl}/`, whisperProcess, 120000, signal);\n"],[391,392,"async function startLlamaServer(executablePath, modelPath, projectorPath, signal) {\n"],[426,427,"    await waitForServer(`${llamaBaseUrl}/health`, llamaProcess, 30 * 60 * 1000, signal);\n"],[436,436,"        if (/\\.en$/.test(whisperModel) && currentLanguage !== 'en') throw new Error('The selected local Whisper model is English-only. Select English or use Gemini/Groq for this language.');\n"],[437,439,"            currentSystemPrompt = getSystemPrompt(profile, customPrompt, false);\n"],[448,449,"        await startWhisperServer(nativeFiles.whisperBinaryPath, nativeFiles.whisperModelPath, controller.signal);\n"],[453,454,"        await startLlamaServer(nativeFiles.llamaBinaryPath, nativeFiles.llamaModelPath, nativeFiles.projectorPath, controller.signal);\n"],[480,483,"        // Verified downloads remain cached. Only this attempt's temporary files\n        // are removed by the downloader; cancelling must not delete unrelated files.\n"],[530,531,""]],"after":"af4f2d7dc5a017b6dbe5968fb3900a2818771243c17bb108b9e3ca0d3b1163a3"},{"path":"src/utils/native-ai-runtime.js","before":"8ceefec2afac3812e893ffb119c1bc45ca6f8d7ff18dd392ff23777c969a3b0e","edits":[[0,0,"const { listModelFiles, selectProjector } = require('./hubMetadata');\n"],[104,104,"    const timeout = AbortSignal.timeout(30 * 60 * 1000);\n    signal = signal ? AbortSignal.any([signal, timeout]) : timeout;\n"],[122,123,"        await pipeline(input, progressStream, fs.createWriteStream(temporaryPath, { flags: 'wx' }), { signal });\n"],[142,155,"    try {\n        const downloadedSha256 = await calculateSha256(temporaryPath);\n    \n        if (downloadedSha256 !== sha256) {\n            fs.rmSync(temporaryPath, { force: true });\n            throw new Error(`Checksum verification failed for ${path.basename(destinationPath)}`);\n        }\n    \n        signal?.throwIfAborted();\n        fs.renameSync(temporaryPath, destinationPath);\n    \n        if (executable && process.platform !== 'win32') {\n            fs.chmodSync(destinationPath, 0o755);\n        }\n    } finally { fs.rmSync(temporaryPath, { force: true }); }\n"],[229,237,"    const files = await listModelFiles(repository, signal);\n"],[239,240,"        return file.type === 'file' && file.path?.toLowerCase().endsWith('.gguf') && file.path.toUpperCase().includes(normalizedQuant) && !file.path.toLowerCase().startsWith('mmproj-');\n"],[251,252,"    const projector = selectProjector(files);\n"],[275,276,"        const projectorName = ['mmproj-BF16.gguf', 'mmproj-F16.gguf', 'mmproj-F32.gguf'].find(name => fs.existsSync(path.join(path.dirname(modelReference), name)));\n        const projectorPath = path.join(path.dirname(modelReference), projectorName || 'mmproj-BF16.gguf');\n"],[339,339,"    childProcess.on('error', error => { childProcess.launchError = error; });\n"],[343,351,"async function waitForServer(url, childProcess, timeoutMs, signal) {\n    const deadline = Date.now() + timeoutMs;\n    while (Date.now() < deadline) {\n        signal?.throwIfAborted();\n        if (childProcess.launchError) throw new Error(`Native server could not start (${childProcess.launchError.code || 'spawn failed'})`);\n        if (childProcess.exitCode !== null || childProcess.killed) throw new Error('Native server stopped before becoming ready');\n        const timeout = AbortSignal.timeout(Math.max(1, Math.min(5000, deadline - Date.now())));\n"],[352,361,"            const response = await fetch(url, { signal: signal ? AbortSignal.any([signal, timeout]) : timeout });\n            await response.body?.cancel();\n            if (response.ok) return;\n        } catch (error) { if (signal?.aborted) throw signal.reason; }\n        await require('node:timers/promises').setTimeout(250, undefined, { signal });\n"],[362,364,"    throw new Error('Native server did not become ready before the startup deadline');\n"]],"after":"eb0dad31a201ef83cc14f9d95e27e879d498327a09fa2c7f2913e6b577df8994"},{"path":"src/utils/providerModelRegistry.js","before":"88ef7e9aa19760450622c26d577ab740aa8ef8031816328d9927051a486368e9","edits":[[91,92,"        preview: raw?.preview === true || /^qwen\\/qwen3\\.(?:6|8)-27b$/.test(id) || isPreviewModel(id),\n"]],"after":"23ac2fabcee492d6d5b6e8d4a33e11800eccebe143f6ff60c8317439bc396bb7"},{"path":"src/utils/renderer.js","before":"fe74c16834be7a56c5b6a8f721bb08f24c7ee8828810131d3926b23841c65130","edits":[[32,32,"const captureContexts = new Set();\nconst videoFrameMetadata = new WeakMap();\n"],[96,97,"function enqueueAudioDispatch(channel, pcmBuffer, sampleRate, epoch, capturedAtMs = Date.now()) {\n"],[102,102,"        capturedAtMs, uiEpoch: contextHaloApp._uiSessionEpoch,\n"],[112,113,"    state.queue.push({ payload, epoch, generation, queuedAt: capturedAtMs });\n"],[117,120,"    // A delay alone is not evidence of a new frame. Compare presentation counters\n    // rather than image hashes: a perfectly static coding screen is still valid.\n    const previous = videoFrameMetadata.get(video);\n"],[121,121,"    let fallbackTimer = null;\n    let finished = false;\n"],[122,124,"        const accept = metadata => {\n            if (finished) return;\n            const index = Number(metadata.presentedFrames);\n            const time = Number(metadata.mediaTime);\n            if (!previous || (Number.isFinite(index) && index > previous.presentedFrames)\n                || (Number.isFinite(time) && time > previous.mediaTime)) {\n                videoFrameMetadata.set(video, { presentedFrames: index, mediaTime: time });\n                resolve(metadata);\n            } else request();\n        };\n        const baseline = Number(video.currentTime);\n        const request = () => {\n            if (finished) return;\n            try {\n                if (typeof video.requestVideoFrameCallback === 'function') {\n                    callbackId = video.requestVideoFrameCallback((_now, metadata) => accept(metadata || {}));\n                } else {\n                    fallbackTimer = setTimeout(() => {\n                        if (Number(video.currentTime) > baseline) accept({ mediaTime: Number(video.currentTime) });\n                        else request();\n                    }, 40);\n                }\n            } catch (error) { reject(error); }\n        };\n        request();\n"],[126,126,"        finished = true;\n        clearTimeout(fallbackTimer);\n"],[245,247,"    // Saved secrets never cross into the renderer. Only replacement values go to main.\n    async getCredentialStatus() {\n"],[248,258,"        const result = await ipcRenderer.invoke('storage:credential-status');\n        if (!result?.success) throw new Error('Credential status could not be loaded.');\n        return result.data;\n"],[261,266,""],[373,374,""],[378,379,"        null, // Legacy argument position; the trusted main process resolves the key.\n"],[421,440,""],[475,476,"            video: { frameRate: 2, width: { ideal: 3840 }, height: { ideal: 2160 } },\n"],[484,485,"        if (!isMacOS && systemAvailable) await setupSystemAudioProcessing(epoch, signal);\n"],[492,493,"                await setupLinuxMicProcessing(micMediaStream, epoch, signal);\n"],[517,518,"async function createCaptureAudioProcessor(stream, channel, epoch, signal) {\n"],[519,519,"    captureContexts.add(context);\n    const check = () => { if (signal.aborted || epoch !== captureEpoch) throw captureAbortError(); };\n"],[520,520,"        check();\n"],[523,524,"        await waitForCapture(context.audioWorklet.addModule(AUDIO_WORKLET_MODULE), signal, 5000);\n        check();\n"],[526,526,"            channelCount: 1, channelCountMode: 'explicit', channelInterpretation: 'speakers',\n"],[531,532,""],[534,535,"            const data = event?.data;\n            if (Number.isSafeInteger(data?.sequence)) processor.port.postMessage({ ack: data.sequence });\n            const pcm = data?.pcm;\n"],[536,537,"            const ageMs = Number.isFinite(data?.audioTime) && Number.isFinite(context.currentTime)\n                ? Math.max(0, (context.currentTime - data.audioTime) * 1000) : 0;\n            if (ageMs > MAX_AUDIO_DISPATCH_AGE_MS) return;\n            enqueueAudioDispatch(channel, pcm, context.sampleRate, epoch, Date.now() - ageMs);\n"],[538,538,"        processor.onprocessorerror = () => {\n            if (epoch === captureEpoch) scheduleCaptureRecovery('audio', epoch);\n        };\n        check();\n"],[542,543,"        captureContexts.delete(context);\n        try { if (context.state !== 'closed') await context.close(); } catch {}\n"],[547,548,"async function setupLinuxMicProcessing(micStream, epoch, signal) {\n"],[554,555,"    const created = await createCaptureAudioProcessor(micStream, 'send-mic-audio-content', epoch, signal);\n"],[559,560,"async function setupSystemAudioProcessing(epoch, signal) {\n"],[566,567,"    const created = await createCaptureAudioProcessor(mediaStream, 'send-audio-content', epoch, signal);\n"],[616,617,"        const scale = Math.min(1, maxWidth / sw, 4096 / sh, Math.sqrt(8_000_000 / (sw * sh)));\n"],[667,668,"    for (const context of captureContexts) {\n"],[671,671,"    captureContexts.clear();\n"],[678,679,"    if (hadAudio) void ipcRenderer.invoke('audio-stream-end', { uiEpoch: contextHaloApp._uiSessionEpoch }).catch(() => {});\n"],[1084,1085,""]],"after":"e2a0a3d947c85ec8a6d9a53d902f043a86b80224ae70f9f753b56118ddc1acdf"},{"path":"src/utils/requestDeadline.js","before":null,"content":"function abortable(work, signal) {\n    if (signal.aborted) return Promise.reject(signal.reason);\n    let onAbort;\n    const cancelled = new Promise((_, reject) => {\n        onAbort = () => reject(signal.reason);\n        signal.addEventListener('abort', onAbort, { once: true });\n    });\n    return Promise.race([Promise.resolve().then(() => {\n        signal.throwIfAborted();\n        return work();\n    }), cancelled]).finally(() => signal.removeEventListener('abort', onAbort));\n}\n\nfunction deadlineSignal(parent, timeoutMs) {\n    const controller = new AbortController();\n    const timer = setTimeout(() => controller.abort(Object.assign(new Error('Provider operation timed out'), {\n        name: 'TimeoutError',\n    })), Math.max(1, timeoutMs));\n    const signal = parent ? AbortSignal.any([parent, controller.signal]) : controller.signal;\n    return {\n        signal,\n        close() {\n            clearTimeout(timer);\n            controller.abort(Object.assign(new Error('Operation finished'), { name: 'AbortError' }));\n        },\n    };\n}\nmodule.exports = { abortable, deadlineSignal };\n","after":"fb827aa821ae92d9ae3ab8657b9b7f50da804b982fe08092ed9d90ee3ef5c090"},{"path":"src/utils/runtimeHardeningMain.js","before":"9ceb9750fc14d0525172ce4ee2abc25beefb7c4182b011ecfe5a54bd3489bfe8","edits":[[382,382,"    resetRuntimeAudio: resetGroqVad,\n"]],"after":"8be389ce66fa56d44b4ea7cbc6c6137a919ec8d691ad51ecb4891559e5e3cedb"},{"path":"src/utils/sse.js","before":"b1cd3424606655881866a22ec693b0a72adbcc7348afdcb8d3d69487897bccc9","edits":[[8,8,"    let dataSize = 0;\n    let ended = false;\n"],[10,12,"        data = []; dataSize = 0;\n        if (value === '[DONE]') { ended = true; return null; }\n        if (!value) return null;\n"],[18,19,"        if (value.startsWith('data:')) {\n            dataSize += value.length;\n            if (dataSize > 2 * 1024 * 1024) throw new Error('Provider SSE event is too large');\n            data.push(value.slice(5).replace(/^ /, ''));\n        }\n"],[34,34,"                if (ended) return;\n"]],"after":"c014577f29bb68b5bb12fd7a2fb1e8d9a95f86d18ac3849fe7c8693e3761e36d"}]