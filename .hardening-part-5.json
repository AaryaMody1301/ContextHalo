[{"path":"src/utils/transportLogger.js","before":"5b2acc875626899e1c363f7b183f3401c510e6b9eda915d840abd251021ba748","edits":[[11,11,"    try {\n"],[18,18,"    } catch { closeTransportLog(); }\n"],[25,26,"    try { logStream.write(line); } catch { closeTransportLog(); }\n"],[28,29,"    if (logStream) { const stream = logStream; logStream = null; try { stream.end(); } catch {} }\n"]],"after":"7e599abae4805de3e1982a56ad0647c7aa852161f09071cb647432435849df63"},{"path":"src/utils/windowsLocalAiRuntime.js","before":"e185abaf4737c244a23d98a7b27584578999f2998912f8c2f7023bb90b569be3","edits":[[0,0,"const { listModelFiles, selectProjector } = require('./hubMetadata');\n"],[43,44,"        signal: signal ? AbortSignal.any([signal, AbortSignal.timeout(15000)]) : AbortSignal.timeout(15000),\n"],[77,77,"    const timeout = AbortSignal.timeout(30 * 60 * 1000);\n    signal = signal ? AbortSignal.any([signal, timeout]) : timeout;\n"],[93,94,"        await pipeline(input, progress, fs.createWriteStream(temporaryPath, { flags: 'wx' }), { signal });\n"],[100,101,"        signal?.throwIfAborted();\n"],[111,116,"    const files = await listModelFiles(repository, signal);\n"],[128,129,"    const projectorFile = selectProjector(files);\n"]],"after":"3e7ff6e21581c18a0b5a005b5ca88304b1fc49c61d9e74cd40d682d33086b7de"},{"path":"src/utils/windowsRuntimeMain.js","before":"8fa52056f179e4458ec98c5faf6f139c3dfe6f5f99608b32c7d5f7a79d07df5e","edits":[[13,13,"let sessionEpoch;\nlet acceptingAudio = false;\n"],[79,80,"    mixedAudioDispatchQueue.push({ event, payload, generation, queuedAt: Number.isFinite(payload.capturedAtMs) ? payload.capturedAtMs : Date.now() });\n"],[111,112,"    const queuedAt = Number.isFinite(payload.capturedAtMs) ? payload.capturedAtMs : Date.now();\n    if (Date.now() - queuedAt > MAX_MIXED_DISPATCH_AGE_MS) return { success: true, dropped: true };\n    const entry = { event, payload, queuedAt };\n"],[116,116,"        const skew = systemAudioQueue[0].queuedAt - microphoneAudioQueue[0].queuedAt;\n        if (Math.abs(skew) > 200) {\n            const unpaired = (skew < 0 ? systemAudioQueue : microphoneAudioQueue).shift();\n            dispatchMixedPayload(unpaired.event, unpaired.payload);\n            continue;\n        }\n        if (systemAudioQueue[0].payload.mimeType !== microphoneAudioQueue[0].payload.mimeType) {\n            // Never interpret a differently sampled track as time-aligned PCM.\n            const unpaired = systemAudioQueue.shift(); microphoneAudioQueue.shift();\n            dispatchMixedPayload(unpaired.event, unpaired.payload);\n            continue;\n        }\n"],[124,124,"            ...systemEntry.payload,\n            capturedAtMs: Math.min(systemEntry.queuedAt, microphoneEntry.queuedAt),\n"],[133,134,"function prepareWindowsProvider(mode, uiEpoch) {\n    sessionEpoch = uiEpoch;\n    acceptingAudio = true;\n"],[145,145,"            acceptingAudio = false;\n"],[153,153,"        };\n    }\n\n    if (channel === 'audio-stream-end') {\n        return async (event, payload) => {\n            if (sessionEpoch !== undefined && payload?.uiEpoch !== sessionEpoch) return { success: true, ignored: true };\n            resetAudioMixer();\n            require('./runtimeHardeningMain').resetRuntimeAudio();\n            return handler(event, payload);\n"],[177,177,"            if (!acceptingAudio || (sessionEpoch !== undefined && payload?.uiEpoch !== sessionEpoch)) return { success: true, ignored: true };\n"],[193,194,"    ipcMain.handle = (channel, handler) => {\n        const wrapped = wrapWindowsIpcHandler(channel, handler);\n        return originalIpcHandle(channel, (event, ...args) => {\n            const window = BrowserWindow.getAllWindows().find(candidate => !candidate.isDestroyed()\n                && candidate.webContents.id === event?.sender?.id);\n            if (!window || event.senderFrame !== window.webContents.mainFrame) return { success: false, error: 'Untrusted renderer' };\n            return wrapped(event, ...args);\n        });\n    };\n"]],"after":"abc4ce485d69b62effc9e5113f25b71ad3dd87b54acf7d6c49e66e03f616f00e"},{"path":"tests/context-behavior.test.js","before":"ab6eccf8a00fd2d3575bf93b5d38bb404081d43e9bc497ce3480abf21b85fe70","edits":[[51,52,"    const selected=await new Promise(resolve=>capture({ frame, audioRequested: true },resolve));assert.equal(selected.video.id,'screen:2:0');assert.equal(selected.audio,'loopback');\n"],[96,97,"        const result = await new Promise(resolve => capture({ frame: mainWindow.webContents.mainFrame, audioRequested: true }, resolve));\n"]],"after":"7bfe8105c8d81593685bf1f0a2f5590b81e8927507f4342ff79ccebe39a26c87"}]