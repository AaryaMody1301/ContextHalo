name: Apply Settings connectivity diagnostics

on:
  push:
    branches:
      - fix/gemini-screen-reliability
    paths:
      - .github/workflows/apply-settings-connectivity-diagnostics.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v7
        with:
          ref: fix/gemini-screen-reliability
          fetch-depth: 0
      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 24
      - name: Apply connectivity diagnostics
        run: node .github/repair-data/settings-connectivity-diagnostics.cjs
      - name: Validate source syntax
        run: node --check scripts/renderer-behavior-smoke.js
      - name: Remove one-time machinery
        run: |
          rm -f .github/repair-data/settings-connectivity-diagnostics.cjs
          rm -f .github/workflows/apply-settings-connectivity-diagnostics.yml
      - name: Commit diagnostics
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "test: expose mounted Settings connectivity"
          git push origin HEAD:fix/gemini-screen-reliability
