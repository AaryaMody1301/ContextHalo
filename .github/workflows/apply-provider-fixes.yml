name: Provider Fix Runner

on:
  push:
    branches: [phase-1-complete-fixes]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: node --check src/utils/gemini.js
